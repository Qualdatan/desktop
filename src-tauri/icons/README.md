# Icons placeholder

Die echten Icon-Dateien (`32x32.png`, `128x128.png`, `icon.ico`) werden
von Phase H (H-BUILD) erzeugt bzw. eingecheckt. Tauri-Build erwartet sie
an diesem Pfad laut `tauri.conf.json`.
